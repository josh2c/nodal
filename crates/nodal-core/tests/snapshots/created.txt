  unit      worker-import  (nodal/worker-import)
  home      /home/j/.nodal/project/e/01J9X2K4
  ports     app :41230
  for       worker import: handle missing supervisor_id
  no value  DATABASE_URL
            STRIPE_KEY
  kept      test-results is excluded by default (test output), but the commit tracks it, so the copy keeps it
