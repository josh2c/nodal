{
  "$defs": {
    "Digest": {
      "description": "A content digest, lowercase hex. The algorithm is the fingerprint module's\nchoice; the model only fixes the form.",
      "maxLength": 128,
      "pattern": "^[0-9a-f]+$",
      "type": "string"
    },
    "ProjectId": {
      "description": "Identifies a project: one Git repository Nodal manages units for.",
      "maxLength": 26,
      "pattern": "^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
      "type": "string"
    },
    "ProjectName": {
      "description": "A project's display name, taken from its directory or its remote.",
      "maxLength": 255,
      "pattern": "^[^\\x00-\\x1F\\x7F-\\x9F]+$",
      "type": "string"
    },
    "RemoteUrl": {
      "description": "What repository a project is, in the one spelling every clone of it agrees on.\n\nIt is the normalised form of a remote URL and never the URL as somebody typed\nit: `git@github.com:josh2c/nodal.git` and `https://github.com/josh2c/nodal`\nare one repository, so they are one of these\n([`crate::git::remote::identity`]).",
      "maxLength": 255,
      "pattern": "^[^\\x00-\\x1F\\x7F-\\x9F]+$",
      "type": "string"
    },
    "Timestamp": {
      "description": "A UTC instant, serialised as an RFC 3339 string.",
      "format": "date-time",
      "type": "string"
    }
  },
  "$id": "https://github.com/josh2c/nodal/blob/main/schemas/v1/project.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A repository Nodal knows about, and the recipe it was last read with.",
  "properties": {
    "created_at": {
      "$ref": "#/$defs/Timestamp",
      "description": "When Nodal first saw this project."
    },
    "id": {
      "$ref": "#/$defs/ProjectId",
      "description": "Identity of the project."
    },
    "name": {
      "$ref": "#/$defs/ProjectName",
      "description": "Display name; the handle a user sees in `nodal ls`."
    },
    "recipe_hash": {
      "$ref": "#/$defs/Digest",
      "description": "Digest of the effective recipe, so a changed `nodal.toml` is visible without\nre-reading the file."
    },
    "remote_url": {
      "anyOf": [
        {
          "$ref": "#/$defs/RemoteUrl"
        },
        {
          "type": "null"
        }
      ],
      "description": "Which repository this is, when it has a remote called `origin`.\n\nThis is the project's identity where it has one, and the checkout path is the\nidentity where it has none. A repository that has never been pushed is an\nordinary project and carries `None`."
    },
    "root": {
      "description": "Absolute path of the repository root on this host.\n\nIt is where the project was first seen. On a host two people share, each of them\nhas a clone of their own, and the row records the first: identity comes from\n[`Project::remote_url`], so a second clone joins the project rather than making\none. Every operation acts on the checkout the person is actually in\n(`lifecycle::ops::new::ensure_project`).",
      "type": "string"
    }
  },
  "required": [
    "id",
    "root",
    "name",
    "recipe_hash",
    "created_at"
  ],
  "title": "Project",
  "type": "object"
}
