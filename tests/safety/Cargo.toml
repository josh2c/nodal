[package]
name = "nodal-safety"
description = "The safety properties of Nodal, asserted on the fixture project as facts CI keeps true."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "nodal_safety"
path = "src/lib.rs"

[dependencies]
# The one call that sets a modification time without following a link, which the
# clone-identity suite needs to give a fixture tree times of its own.
libc.workspace = true
nodal-core.workspace = true
nodal-fixture.workspace = true
serde_json.workspace = true
tempfile.workspace = true

[lints]
workspace = true
